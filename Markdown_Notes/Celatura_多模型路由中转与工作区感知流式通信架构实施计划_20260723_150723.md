# Celatura 多模型路由中转、工作区感知与 SSE 流式通信架构实施计划

### [2026-07-23 15:07:23] 数据大动脉重构实施计划

---

## 架构与核心逻辑设计

```mermaid
flowchart TD
    A[前端 ChatStreamView] -->|1. 包含 Workspace/Model/Prompt 发起任务| B(Tauri 2 IPC: execute_llm_task)
    B --> C{检查 Workspace Path}
    C -->|存在路径| D[自动扫描 top-level 目录 & 提取 package.json/Cargo.toml 结构 Context]
    C -->|未指定| E[拼装标准 Prompt]
    D --> E
    E --> F{分发 Model Provider}
    F -->|Gemini REST SSE| G[streamGenerateContent 端点]
    F -->|DeepSeek / OpenAI SSE| H[/v1/chat/completions stream:true]
    F -->|CLI 兼容回退| I[系统 gemini CLI 管道]
    G & H & I -->|实时 Token 块| J[Tauri Event: gemini-stream]
    J -->|Chunk 追加渲染| A
```

---

## 拟修改文件与架构规划

- **[MODIFY] [Cargo.toml](file:///d:/AI_Tools/Celatura-desktop/src-tauri/Cargo.toml)**：为 `reqwest` 补充 `stream` 特性，引入 `futures-util`。
- **[MODIFY] [lib.rs](file:///d:/AI_Tools/Celatura-desktop/src-tauri/src/lib.rs)**：实现 `build_workspace_context` 扫描函数、`execute_llm_task` 多模型 SSE 流路由与分发。
- **[MODIFY] [ChatStreamView.tsx](file:///d:/AI_Tools/Celatura-desktop/src/components/ChatStreamView.tsx)**：精美工作区选中展示、流式 Token 实时广播监听与气泡渲染。

---

## 验证规划

- 运行 `cargo check` 与 `npm run build`。
- 走查工作区结构感知、多提供商流式响应及代码高亮复制功能。
